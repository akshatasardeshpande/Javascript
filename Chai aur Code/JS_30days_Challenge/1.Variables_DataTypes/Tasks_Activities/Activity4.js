//Activity 4: Reassigning variables
//Task 5: Declare a variable using let, assign it an initial value, reassign a new value, and log both values to the console.

let reassignTest = 'akshata';
console.log(`${reassignTest}`);

reassignTest ='hitesh';
console.log(`${reassignTest}`);